## Day 41 – Date Arithmetic (INTERVAL)

### Problem Statement

You are given a table `orders` with the following columns:

- order_id (INT)
- customer_id (INT)
- order_date (DATE)
- delivery_date (DATE)
- amount (DECIMAL)

Write SQL queries to:

1. Add days to a date using INTERVAL
2. Subtract days from a date
3. Calculate the difference between two dates
4. Find orders delivered within a specific number of days
5. Understand date arithmetic using INTERVAL

Focus on **date calculations and time differences**.
