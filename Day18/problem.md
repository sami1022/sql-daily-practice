## Day 18 – CHECK & DEFAULT (Validation Constraints)

### Problem Statement

You are designing a table to store employee information.

The table must ensure:
- Certain columns accept only valid values
- Default values are automatically assigned when no value is provided
- Data validation is enforced at the database level

Perform the following tasks:

1. Create a table using CHECK constraints
2. Apply DEFAULT values to appropriate columns
3. Insert valid records using DEFAULT values
4. Attempt to insert invalid data that violates CHECK constraints
5. Understand how CHECK and DEFAULT improve data quality

Focus on validation and default value handling.
