Added basic SELECT and WHERE SQL queries
Create a table called `students` and insert sample data.
Write queries to:
1. Select all students
2. Filter students with marks greater than 75
3. Sort students by marks in descending order
