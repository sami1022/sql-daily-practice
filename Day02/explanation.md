### Explanation

- The WHERE clause is used to filter rows based on conditions.
- AND operator ensures that **all conditions** must be true.
- OR operator returns rows where **at least one condition** is true.
- String values like city names must be enclosed in single quotes.

These operators are commonly used in interview and real-world data filtering scenarios.
